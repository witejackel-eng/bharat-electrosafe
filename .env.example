# ────────────────────────────────────────────────────────────────
# Bharat Electrosafe — Environment Variables
# ────────────────────────────────────────────────────────────────

# NEXT_PUBLIC_SITE_URL
# The final canonical domain of the public-facing website.
# Must always be the permanent production domain in the production environment.
# Preview and local deployments may use a different URL, but canonical URLs,
# sitemap entries, robots.txt, Open Graph and JSON-LD all resolve against this
# value when indexing is enabled.
NEXT_PUBLIC_SITE_URL=https://bharatelectrosafe.com

# NEXT_PUBLIC_ALLOW_INDEXING
# Set to "true" ONLY in the production environment.
# Keep "false" (or unset) for local development, preview deployments and
# staging. When false, every page emits noindex/nofollow metadata and
# robots.txt disallows all crawling — this prevents search engines from
# accidentally indexing non-production URLs.
NEXT_PUBLIC_ALLOW_INDEXING=false

# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION
# Google Search Console verification token. Only output when a real value
# exists. Do not commit a real verification token — add it in the Vercel
# dashboard when ready. Leave empty until the production domain is verified.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
